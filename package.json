{
  "name": "code-sync",
  "version": "1.0.0",
  "description": "Code synchronization application",
  "scripts": {
    "start": "cd server && npm start",
    "heroku-postbuild": "cd client && npm install && npm run build && cd ../server && npm install"
  },
  "engines": {
    "node": "18.x"
  }
}